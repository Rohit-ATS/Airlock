# AIRLOCK — everything here is optional. A fresh clone runs with none of it set.

# ---------------------------------------------------------------------------
# The harness
# ---------------------------------------------------------------------------
# Where the TrueForge server is. Local mode is 8790, hosted mode is 8791.
# On Windows, TrueForge 0.1.4 does not start natively — use Docker.
# NEXT_PUBLIC_TRUEFORGE_BASE_URL=http://localhost:8791

# Server-side copy, used by the console's own API routes to resolve who is
# asking. Falls back to the public value above.
# TRUEFORGE_BASE_URL=http://localhost:8790

# ---------------------------------------------------------------------------
# The model the agent thinks with
# ---------------------------------------------------------------------------
# Required for the agent path (`npm run harness:turn`). Not required for the
# console, the fixtures, or `npm run demo` -- the demo drives AIRLOCK's own MCP
# server, so it proves the gate without a model key.
#
# NOT read from the environment by TrueForge. Model credentials are runtime
# settings there, which is the single most confusing thing about standing this
# up by hand. Put the key here and run `npm run harness:setup`, which registers
# it for you along with the models below.
#
# WHICH MODELS, AND WHY IT MATTERS. TrueForge ships no model catalog --
# `/api/v1/models` returns exactly what harness-setup.mjs registered. It used to
# register the 4.1 family, and that was the largest single cause of "AIRLOCK does
# not work". Measured on a real account:
#
#     gpt-4.1        30,000 TPM     <- a change-control run cannot fit through this
#     gpt-4.1-mini  200,000 TPM
#     gpt-5-mini    500,000 TPM     <- the scout tier
#     gpt-5.2       500,000 TPM     <- the primary tier
#
# One iteration costs ~8.1k input tokens, so on 30k TPM a real run is throttled
# every third or fourth step and often dies mid-investigation. If your account's
# ceilings differ, change the list in scripts/harness-setup.mjs -- it is four
# lines and it is the thing to check first when a run keeps dying on a 429.
# OPENAI_API_KEY=sk-proj-...
#
# An Anthropic key works instead; harness-setup falls back to it and registers
# claude-sonnet-4-6 and claude-haiku-4-5.
# ANTHROPIC_API_KEY=sk-ant-...

# ---------------------------------------------------------------------------
# The sandbox
# ---------------------------------------------------------------------------
# Where the shadow branch runs and every checksum in this project is produced.
# No sandbox means the agent can plan a migration and can never prove one, so
# this is load-bearing rather than optional.
#
# TrueForge 0.1.4 accepts exactly one provider — the server validates the
# manifest type against the literal string "daytona" — so there is no local or
# Docker-backed fallback. A key is free at https://app.daytona.io (Keys ->
# Create Key). Add it here, then run: npm run harness:sandbox
# DAYTONA_API_KEY=dtn_...

# ---------------------------------------------------------------------------
# The ledger
# ---------------------------------------------------------------------------
# Where the change ledger is written. A judge can read it with `cat`.
# AIRLOCK_DATA_DIR=./.airlock

# Required for machine writes when the console runs in production mode. The
# SQLite verifiers and MCP server send the same token as `Authorization: Bearer`.
# AIRLOCK_API_TOKEN=

# Required for the HTTP MCP transport. TrueForge sends this bearer token when it
# calls AIRLOCK's remote MCP server; `npm run up` generates one for local demos.
# Keep it distinct from AIRLOCK_API_TOKEN so exposing MCP does not expose the
# console's own machine-write credential.
# AIRLOCK_MCP_HTTP_TOKEN=

# Start with an empty queue instead of seeding the eighteen console fixtures.
# AIRLOCK_NO_SEED=1

# Where the seed fixtures are read from. Normally found automatically, relative
# to the console's own module and then to the working directory. Set this when
# the console is started by something that chooses its own working directory —
# a service manager, a container entrypoint, `next start <dir>` — where the
# search would otherwise walk an unrelated branch of the filesystem, find
# nothing, and seed an empty queue without saying why.
# AIRLOCK_FIXTURES_DIR=./contracts/examples

# ---------------------------------------------------------------------------
# Who is allowed to approve
# ---------------------------------------------------------------------------
# Roles come from the harness (`GET /api/v1/auth/me`). When the harness cannot
# answer, AIRLOCK separates two cases that look the same and are not:
#
#   nothing configured        -> a single `local-operator` who may approve, with
#                                a permanent banner saying there is no identity
#                                provider and this is not separation of duties
#   configured, not answering -> `requester`; the gate stays shut, because a
#                                dropped container must never mint an approver
#
# Unset (the default) picks between those by whether TRUEFORGE_BASE_URL or
# NEXT_PUBLIC_TRUEFORGE_BASE_URL is set at all.
#
#   0 = never grant the local operator role, even standalone
#   1 = always grant it, even when a harness is configured
# AIRLOCK_LOCAL_OPERATOR=0

# ---------------------------------------------------------------------------
# Local SQLite verifier
# ---------------------------------------------------------------------------
# First-pass verification target for Damir's workstream. The verifier copies
# this file to a shadow SQLite database, applies forward SQL, rolls it back,
# compares checksums, and posts the resulting dossier to the console.
# SQLITE_PATH=./data/airlock.sqlite
# AIRLOCK_SEED_ROWS=50000
# AIRLOCK_DOSSIER_ID=dos_sqlite_tier_demo
# AIRLOCK_REQUESTED_BY=damir@airlock.dev

# ---------------------------------------------------------------------------
# Supabase -- the database the demo proves things against
# ---------------------------------------------------------------------------
# Needed for `npm run seed:supabase` and for the live demo. Without them the
# verifier falls back to the local SQLite file above, which is a real
# measurement of a database that is not the one the change is destined for.
#
# TWO CREDENTIALS, DELIBERATELY DIFFERENT.
#
# The seeder writes, through the management API, with a personal access token --
# a human's admin credential, used by a human, from a terminal. The AGENT never
# sees it. The agent reads the same database through
# `mcp.supabase.com/mcp?project_ref=...&read_only=true`, where Postgres refuses
# a write itself:
#
#     ERROR: 25006: cannot execute CREATE TABLE in a read-only transaction
#
# Setting the demo up and running the demo are not the same privilege, and that
# separation is the point rather than an inconvenience.
#
# Use a project you do not mind seeding: `--reset` DROPS the six demo tables and
# rebuilds them. Point this at anything you care about and you will lose it.
#
# SUPABASE_URL is a project URL from Settings -> API. The project ref is its
# subdomain, and is derived from this, so SUPABASE_PROJECT_REF is only needed if
# you would rather set it explicitly.
# SUPABASE_URL=https://<project-ref>.supabase.co
# SUPABASE_PROJECT_REF=<project-ref>
#
# A personal access token from https://supabase.com/dashboard/account/tokens.
# This is NOT the anon key and NOT the service role key.
# Supabase preview branches require Pro or above. On Free-tier projects AIRLOCK
# still verifies live Postgres data through the throwaway-schema fallback.
# SUPABASE_ACCESS_TOKEN=sbp_...
#
# How many users to seed. Everything else fans out from it: the default lands
# 1,000,000 rows across six tables at about 135 MB, which fits the free tier
# with room to spare. Verify any time with:  npm run seed:supabase -- --check
# AIRLOCK_SEED_USERS=100000

# ---------------------------------------------------------------------------
# Break-glass
# ---------------------------------------------------------------------------
# Two switches are required, and this is the deployment one: the change class
# must also permit it in policy. Off by default, deliberately.
#
# Break-glass does not open the gate — it cannot mint an approval grant. It
# records that a named human went around a sealed door, with a written reason,
# permanently, in the same hash chain as everything else.
# AIRLOCK_BREAK_GLASS=1

# ---------------------------------------------------------------------------
# The GitHub webhook
# ---------------------------------------------------------------------------
# Without this the webhook route refuses every request with a 503 rather than
# accepting unsigned payloads. There is no development bypass.
# GITHUB_WEBHOOK_SECRET=
#
# Which repositories the webhook will call the GitHub API about, comma
# separated. The repository named in the payload has to match one of these, and
# the entry configured here — not the one that arrived — is what the API URL is
# built from. That is the difference between validating a value and not
# trusting it: a webhook body cannot choose where this server sends a request.
# Falls back to GITHUB_REPOSITORY, which Actions sets for you.
# GITHUB_REPO=Rohit-ATS/Airlock

# ---------------------------------------------------------------------------
# The MCP server (packages/mcp)
# ---------------------------------------------------------------------------
# Where the AIRLOCK console is, from the agent's point of view.
# AIRLOCK_CONSOLE_URL=http://localhost:3000
